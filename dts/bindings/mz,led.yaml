# Description of drivers
description: MZ module, LED

# Compatibility string, match with DTS and C (DT_DRV_COMPAT)
compatible: "mz,led"

# Define node properties
properties:
    pin:
        type: phandle
        required: true