# Description of drivers
description: Zephyr Module, GPIO

# Compatibility string, matches with DTS and C (DT_DRV_COMPAT)
compatible: "m,gpio"

# Define node properties
properties:
    pin:
        type: phandle
        required: true

