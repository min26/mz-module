name: mz_module

build:
  ## path to Kconfig file
  kconfig: Kconfig

  ## path to folder of CMakeLists.txt
  cmake: .

  settings:
    ## additional board roots
    board_root: .
    
    ## additional dts/bindings, if need.
    dts_root: .
